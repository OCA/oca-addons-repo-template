[dependency-groups]
dev = [
    "copier>=9.14.3",
    "pytest",
    "pre-commit",
    "pytest-xdist",
]

# ruff

[tool.ruff]
fix = true

[tool.ruff.lint]
extend-select = [
    "UP",  # pyupgrade
    "I",  # isort
]

# pytest

[tool.pytest.ini_options]
addopts = "-n auto -ra tests"
